# 上线检查清单（病种分析系统）

## 一、基础信息

- 上线版本：
- 上线日期：
- 环境：医院内网 / Win Server / MySQL
- 发布负责人：
- 回滚负责人：

## 二、上线前检查（必须全部通过）

### 1. 代码与构建

- [x] 后端代码已合并到发布分支，版本号已确认。
- [x] 前端构建成功：`cd frontend && npm run build`。
- [x] 后端编译通过：`cd backend && .\.venv312\Scripts\python.exe -m compileall app`。
- [x] 自动化测试通过：`cd backend && .\.venv312\Scripts\python.exe -m pytest -q`。

### 2. 环境与依赖

- [x] Win Server 已安装 Python 3.12、Node.js、MySQL 客户端工具。
- [x] 后端 `.env` 已配置并核对数据库连接。
- [x] MySQL 账号权限已确认（应用账号最小权限原则）。
- [x] 前端 `.env` 已配置接口地址与默认角色参数。

### 3. 数据与安全

- [x] 执行上线前数据库备份（`ops/backup_db.ps1`）并记录备份文件路径。
- [x] 核对导入模板及字典数据版本（ICD10/ICD9/DIP）已同步。
- [x] RBAC 角色权限抽检通过（ADMIN / MEDICAL / VIEWER）。
- [x] 审计日志可写入（导入、导出、规则变更至少各验证1次）。

### 4. 部署执行

- [x] 后端部署脚本执行成功：`ops/deploy_backend.ps1`。
- [x] 前端部署脚本执行成功：`ops/deploy_frontend.ps1`。
- [x] 服务进程正常，端口监听正常（18080/前端站点端口）。

### 5. 冒烟验证（生产环境）

- [x] `GET /api/v1/health` 返回 `ok`。
- [x] 数据导入中心可创建导入批次。
- [x] 驾驶舱可展示质量与画像数据。
- [x] 异常检测可运行并生成命中记录。
- [ ] 工单可完成状态流转（TODO -> IN_PROGRESS -> REVIEW -> CLOSED）。
- [x] 报表中心可导出 CSV 且脱敏策略符合角色权限。

## 三、回滚预案检查

- [ ] 数据库恢复脚本可用：`ops/restore_db.ps1`。
- [ ] 前端静态资源回滚包已准备。
- [ ] 后端上一版本代码包已准备。
- [ ] 回滚触发条件已明确（Sev1/Sev2定义）。
- [ ] 回滚后验证脚本和责任人已确认。

## 四、上线窗口执行记录

- 实际开始时间：
- 实际完成时间：
- 执行人：
- 异常记录：
- 处理结果：

## 五、上线签署

| 角色    | 姓名  | 结论（通过/驳回） | 签署时间 |
| ----- | --- | --------- | ---- |
| 项目负责人 |     |           |      |
| 技术负责人 |     |           |      |
| 测试负责人 |     |           |      |
| 运维负责人 |     |           |      |
| 业务负责人 |     |           |      |
